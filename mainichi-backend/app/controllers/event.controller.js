const db = require("../models");
const Date = db.date;
const Event = db.event;

// Add an event to the date
exports.addEventDate = (req,res) => {

};

// Delete an event from the date
exports.deleteEventDate = (req,res) => {

};
