const db = require("../models");
const Date = db.date;
const Icon = db.icon;

// Add an icon to the date
exports.addIconDate = (req,res) => {

};

// Delete an icon from the date
exports.deleteIconDate = (req,res) => {

};
